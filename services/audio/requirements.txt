# Audio Service Dependencies

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Speech-to-Text
faster-whisper>=1.0.0
# Alternative: openai-whisper (slower but works without specific dependencies)

# Text-to-Speech
piper-tts>=1.2.0
# Note: Piper requires ONNX runtime
onnxruntime>=1.16.0

# Voice Activity Detection
torch>=2.0.0
torchaudio>=2.0.0
# Silero VAD is loaded from torch hub

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0  # Optional: for advanced audio processing
pyaudio>=0.2.13  # For microphone input
wave  # Built-in Python module

# Optional: Wake word detection
# openwakeword>=0.5.0  # Uncomment if wake word detection is needed

